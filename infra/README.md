# Infrastructure

Infrastructure is intentionally minimal in the first monorepo pass.

Use `apps/api/compose.yaml` for local backend dependencies. Add deployment-specific files here later only when they are needed.
